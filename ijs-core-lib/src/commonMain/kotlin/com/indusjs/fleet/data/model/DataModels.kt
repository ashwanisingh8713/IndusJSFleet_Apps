package com.indusjs.fleet.data.model

/**
 * Base marker interface for Data Transfer Objects (DTOs).
 * DTOs are used for API responses.
 */
interface Dto

/**
 * Base marker interface for database entities.
 * These are Room entities for local storage.
 */
interface DbEntity

