Act as a professional Kotlin Multiplatform Developer.
I am developing Fleet Management mobile app.
Fleet_Management_API_v2.postman_collection.json is PostMan collection.

In `/Users/ashwani/KotlinMultiplateform/IndusJSFleet/sharedUI`, Already app is being developed and apis are getting integrated using CLEAN Architecture, Dependency Injection, MVI design pattern.
Kindly review it.

We have following screens in app.
    Login -> LoginScreen.kt
    Create Account -> SignUpScreen.kt
    Change Password -> ChangePasswordScreen.kt
    Forgot Password -> ForgotPasswordScreen.kt
    Profile -> ProfileScreen.kt

    Dashboard -> DashboardScreen.kt

    Vehicles -> VehiclesScreen.kt
    Vehicle Details -> VehicleDetailsScreen.kt
    Create Vehicle -> CreateVehicleScreen.kt

    Create Trip -> CreateTripScreen.kt
    Trip Details -> TripDetailsScreen.kt
    Add Trip Cost -> TripCostEntryScreen.kt
    Add Vehicle Cost -> MaintenanceCostEntryScreen.kt




 ----------------------------------------


 Review all apis to serve the actual contents from backend server to Mobile app's appropriate screens.
 `/Users/ashwani/GolandProjects/IndusJSFleet_GoLang_Backend` this is path of backend server code written in GoLang.

 Plan with best practices to implement and verify the all required apis into Mobile App.

--------------------------------------------
In this added`Trip cost`, there is no ui to see the each trip cost.
In `TripsScreen.kt` it should show all added cost of each trip.
In `TripDetailScreen.kt` there should be separate UI to see the detailed cost in details for each type of TripCost.

Plan it in best approach and best practice.

