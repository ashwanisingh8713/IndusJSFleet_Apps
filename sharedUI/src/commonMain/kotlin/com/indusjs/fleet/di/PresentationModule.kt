package com.indusjs.fleet.di

/**
 * Presentation layer dependency graph.
 * Provides ViewModels and presentation-level dependencies.
 *
 * Note: In Metro, ViewModels are typically created through feature-specific graphs
 * with constructor injection using @Inject annotation.
 */
object PresentationModule {
    // Presentation layer dependencies (ViewModels) are provided through feature-specific graphs
    // See SampleFeatureGraph for example usage
}

