package com.indusjs.fleet.di

/**
 * Domain layer dependency graph.
 * Provides use cases and domain-level dependencies.
 *
 * Note: In Metro, use cases are typically injected via constructor injection
 * with @Inject annotation. They are created through feature-specific graphs.
 */
object DomainModule {
    // Domain layer dependencies (use cases) are provided through feature-specific graphs
    // See SampleFeatureGraph for example usage
}

